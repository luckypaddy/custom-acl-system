# The Interactive-Html-Report generation is enabled by default.
# For load testing this report may not be quite useful as we are
# mostly interested in load statistics which we can get from the CSV reports.
# Also the HTML interactive reports particularly takes bit longer to generate during load testing.
interactive.html.report.disabled=true